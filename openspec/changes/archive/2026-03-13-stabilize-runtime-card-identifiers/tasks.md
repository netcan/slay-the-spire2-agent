## 1. 运行时身份建模

- [x] 1.1 梳理当前 live 手牌可用的实例字段、位置字段与现有 `card_id` 生成逻辑。
- [x] 1.2 实现稳定的实例级 `card_id` 生成策略，并覆盖重复手牌场景。
- [x] 1.3 调整 `play_card` action 构建逻辑，让 `params.card_id` 与 `action_id` 体现实例差异。

## 2. 执行链路对齐

- [x] 2.1 修改 `apply play_card` 的执行定位逻辑，按实例级 `card_id` 匹配具体手牌。
- [x] 2.2 在 `card_id` 已失效或无法匹配时返回明确拒绝结果，而不是模糊回退。

## 3. 验证与回归

- [x] 3.1 补充单元测试，覆盖重复同名牌和 `action_id` 去冲突场景。
- [x] 3.2 在真实 STS2 战斗中验证两张 `打击` / 两张 `防御` 拥有不同 `card_id` 与 `action_id`。
